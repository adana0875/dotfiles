
useful cmds
`shift + v` visual line mode
`cmd + cd ` enter file explorer
`%` in file explorer = new file
`c + i + <character>` delete/move to spot in insert
`shift + o` new line above current (`o` does below)